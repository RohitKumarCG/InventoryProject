@model Inventory.Mvc.Models.ProductViewModel

@{
    Layout = "~/Views/Shared/SystemUserLayout.cshtml";
}

<div class="container-fluid">
    <div class="mx-auto">
        <h3 style="color:#808080">Edit Product</h3>

        @using (Html.BeginForm("Edit", "Products", FormMethod.Post))
        {
            @Html.HiddenFor(temp => temp.ProductID)
            @Html.HiddenFor(temp => temp.ProductType)

            <div class="row">

                <div class="col-md-6 mx-auto border-secondary">

                    <div class="form-group form-row">
                        @Html.LabelFor(temp => temp.ProductName, "Product Name",
                            new {@class="col-form-label col-lg-3"})

                        <div class="col-lg-9">
                            @Html.TextBoxFor(temp => temp.ProductName,
                            new{@class= "form-control col-lg-9 productName", placeholder = "Product Name", maxlength = "30" })

                            @Html.ValidationMessageFor(temp => temp.ProductName)
                        </div>
                    </div>

                    <div class="form-group form-row">
                        @Html.LabelFor(temp => temp.ProductCode, "Product Code",
                            new {@class="col-form-label col-lg-3" })

                        <div class="col-lg-9">
                            @Html.TextBoxFor(temp => temp.ProductCode,
                            new{@class= "form-control col-lg-9 productCode", placeholder = "Product Code", maxlength = "5" })

                            @Html.ValidationMessageFor(temp => temp.ProductCode)
                        </div>
                    </div>

                    <div class="form-group form-row">
                        @Html.LabelFor(temp => temp.ProductUnitPrice, "Unit Price",
                            new {@class="col-form-label col-lg-3" })

                        <div class="col-lg-9">
                            @Html.TextBoxFor(temp => temp.ProductUnitPrice,
                            new{@class= "form-control col-lg-9 productPrice", placeholder = "Product Price" })
                            @Html.ValidationMessageFor(temp => temp.ProductUnitPrice)
                        </div>
                    </div>

                    <div class="border-top text-right pt-2">
                        <button class="btn btn-success" id="createButton"
                                onclick="return confirm('Are you sure')">
                            Edit
                        </button>
                    </div>

                </div>
            </div>
        }

    </div>
</div>

<script>
    $(document).ready(function () {
        $(function () {
            $('#createButton').attr('disabled', 'disabled');
        });

        $('input[type=text],input[type=text],input[type=text]').keyup(function () {
            if ($('.productName').val().length > 2 && $('.productCode').val().length > 1 && $('.productPrice').val() != '') {
                $('#createButton').removeAttr('disabled');
            }
            else {
                $('#createButton').attr('disabled', 'disabled');
            }
        });
    });
</script>